1. To install dependencies, run command:
pip install -r requirements.txt
2. Data exploration is in data_exploration.ipynb. To run this notebook, open it.
3. To run the classifier, run the command:
python classifier.py
4. To run the segmentation model, run the command:
python segmentation_model.py